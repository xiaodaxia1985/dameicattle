# 使用Alpine Linux作为基础镜像（更小，更容易下载）
FROM alpine:latest

# 安装Node.js和npm
RUN apk add --no-cache nodejs npm

# 设置工作目录
WORKDIR /app

# 设置npm镜像源
RUN npm config set registry https://registry.npmmirror.com

# 复制共享库
COPY shared/ ../shared/
WORKDIR /shared
RUN npm install && npm run build

# 返回应用目录
WORKDIR /app

# 复制package文件
COPY package*.json ./
RUN npm install

# 复制应用代码
COPY . ./
RUN npm run build

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["npm", "start"]